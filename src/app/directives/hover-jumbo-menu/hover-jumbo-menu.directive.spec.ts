import { HoverJumboMenuDirective } from './hover-jumbo-menu.directive';

describe('HoverJumboMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverJumboMenuDirective();
    expect(directive).toBeTruthy();
  });
});
